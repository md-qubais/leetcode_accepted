class Solution {
public:
    string longestCommonPrefix(vector<string>& strs){
    	string ans="";
    	for(int i=0;i<strs[0].size();i++){
    		char ch=strs[0][i];
    		int count=0;
    		for(int j=0;j<strs[i].size();i++){
    			if(ch==strs[i][]){

    			}
    		}
    	}
    	return ans;
    }
};